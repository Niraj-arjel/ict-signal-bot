name: ICT Signal Bot

on:
  schedule:
    - cron: "*/15 * * * *"   # Runs every 15 minutes
  workflow_dispatch:

jobs:
  signal:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Test workflow
        run: |
          echo "ICT Signal Bot is running"
          date
